name: Fetch BOE diario

on:
  schedule:
    - cron: '0 7 * * 1-5'  # 8:00h Madrid (lunes a viernes)
  workflow_dispatch:        # permite ejecutarlo manualmente

jobs:
  fetch-boe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Fetch RSS del BOE y guardar JSON
        run: |
          python3 << 'EOF'
          import urllib.request
          import xml.etree.ElementTree as ET
          import json
          from datetime import datetime, timedelta
          import email.utils

          fecha_limite = datetime.utcnow() - timedelta(days=7)

          # Solo regulación general — excluir autorizaciones individuales de proyectos
          keywords_incluir = [
              'real decreto', 'orden ministerial', 'circular', 'resolución',
              'reglamento', 'subasta', 'concurso', 'procedimiento de operación',
              'instrucción de operación', 'plan', 'consulta pública',
              'almacenamiento', 'baterías', 'bater', 'bess', 'bombeo',
              'flexibilidad', 'interrumpibilidad', 'agregad', 'demanda',
              'renovable', 'fotovoltai', 'eólica', 'eolica', 'solar',
              'ppa', 'contrato bilateral', 'mercado a plazo', 'omip',
              'acceso', 'conexión', 'red de transporte', 'cnmc', 'ree',
              'hibridación', 'hibridacion', 'energía eléctrica'
          ]

          # Excluir autorizaciones individuales de proyectos
          keywords_excluir = [
              'información pública', 'informacion publica',
              'evaluación de impacto ambiental', 'evaluacion de impacto',
              'concesión de aguas', 'servidumbre', 'expropiación',
              'autorización administrativa previa',
              'autorización de construcción',
          ]

          feeds = [
              ('https://www.boe.es/rss/boe.php?s=1', 'Seccion I - Disposiciones generales'),
              ('https://www.boe.es/rss/boe.php?s=3', 'Seccion III - Otras disposiciones'),
              ('https://www.boe.es/rss/boe.php?s=5', 'Seccion V - Anuncios'),
          ]

          items = []
          errores = []

          for url, seccion in feeds:
              try:
                  req = urllib.request.Request(url, headers={
                      'User-Agent': 'Mozilla/5.0 (compatible; BOE-Monitor/1.0)'
                  })
                  with urllib.request.urlopen(req, timeout=30) as response:
                      content = response.read()

                  root = ET.fromstring(content)

                  for item in root.findall('.//item'):
                      title_el = item.find('title')
                      link_el = item.find('link')
                      pubdate_el = item.find('pubDate')
                      desc_el = item.find('description')

                      if title_el is None or link_el is None:
                          continue

                      titulo = (title_el.text or '').strip()
                      link = (link_el.text or '').strip()
                      descripcion = (desc_el.text or '').strip() if desc_el is not None else ''

                      # Validar fecha
                      fecha_pub = None
                      if pubdate_el is not None and pubdate_el.text:
                          try:
                              fecha_pub = email.utils.parsedate_to_datetime(pubdate_el.text)
                              fecha_pub = fecha_pub.replace(tzinfo=None)
                          except Exception:
                              pass

                      if fecha_pub and fecha_pub < fecha_limite:
                          continue

                      texto = (titulo + ' ' + descripcion).lower()

                      # Filtrar por keywords de exclusión primero
                      if any(kw in texto for kw in keywords_excluir):
                          continue

                      # Filtrar por keywords de inclusión
                      tema = None
                      if any(kw in texto for kw in ['almacenamiento', 'bater', 'bess', 'bombeo', 'hibridaci']):
                          tema = 'BATERIAS'
                      elif any(kw in texto for kw in ['flexibilidad', 'interrumpibilidad', 'agregad', 'ppa', 'contrato bilateral', 'mercado a plazo', 'omip', 'demanda']):
                          tema = 'FLEXIBILIDAD'
                      elif any(kw in texto for kw in ['renovable', 'fotovoltai', 'eólica', 'eolica', 'solar', 'real decreto', 'orden ministerial', 'circular', 'reglamento', 'acceso', 'conexión', 'energía eléctrica']):
                          tema = 'NOVEDADES'

                      if tema is None:
                          continue

                      boe_id = link.split('id=')[-1] if 'id=BOE' in link else link

                      items.append({
                          'id': boe_id,
                          'titulo': titulo,
                          'link': link,
                          'seccion': seccion,
                          'tema': tema,
                          'fecha_publicacion': fecha_pub.strftime('%Y-%m-%d') if fecha_pub else '',
                          'descripcion': descripcion[:300]
                      })

              except Exception as e:
                  errores.append({'feed': url, 'error': str(e)})

          resultado = {
              'generado': datetime.utcnow().strftime('%Y-%m-%dT%H:%M:%SZ'),
              'rango_inicio': fecha_limite.strftime('%Y-%m-%d'),
              'rango_fin': datetime.utcnow().strftime('%Y-%m-%d'),
              'total': len(items),
              'errores': errores,
              'items': items
          }

          import os
          os.makedirs('datos', exist_ok=True)
          with open('datos/boe-semana.json', 'w', encoding='utf-8') as f:
              json.dump(resultado, f, ensure_ascii=False, indent=2)

          print(f"OK: {len(items)} items relevantes. Errores: {len(errores)}")
          if errores:
              for e in errores:
                  print(f"  ERROR: {e}")
          EOF

      - name: Commit y push
        run: |
          git config user.email "github-actions@github.com"
          git config user.name "BOE Fetcher"
          git add datos/boe-semana.json
          git diff --staged --quiet || git commit -m "data: BOE $(date +%Y-%m-%d)"
          git push
